apiVersion: v1
kind: Service
metadata:
  name: intent-subscriber
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: intent-subscriber
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081
      protocol: TCP
      name: http
