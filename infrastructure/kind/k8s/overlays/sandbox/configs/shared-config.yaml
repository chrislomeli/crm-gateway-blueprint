
### FILE: k8s/overlays/sandbox/configs/webhook-config.yaml
```yaml
# Webhook subscriber configuration for sandbox
retry:
  enabled: true
  maxAttempts: 3
  backoffSeconds: 5

timeout:
  connectSeconds: 10
  responseSeconds: 30

validation:
  requireHttps: true
  validateCertificates: true

batchSize: 10
maxConcurrent: 5
flushIntervalSeconds: 60
logLevel: "info"
