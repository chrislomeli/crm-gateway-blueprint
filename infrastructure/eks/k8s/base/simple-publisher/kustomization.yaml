apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - service-account.yaml

labels:
  - pairs:
      app.kubernetes.io/name: crm-hubspot-webhook-ingress
      app.kubernetes.io/component: webhook-ingress
