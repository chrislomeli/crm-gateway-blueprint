/**
 * Services Module
 * 
 * This module provides low-level wrappers around external publishing like AWS SDK.
 * It depends only on the core package for logging and result handling.
 */

// Export AWS publishing
export * from './aws';
