/**
 * HTTP Connector exports
 * 
 * This module exports HTTP connector implementations for use in Blueprint applications.
 */

// Export the functional HttpClient (modernized)
export { createHttpClient } from './HttpClient';
export type { HttpClientConfig } from './HttpClient';
